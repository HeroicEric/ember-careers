- makeTree could be plain string, converted by readTree
- improve error messages for es6-concatenator
- make es6-concatenator only emit ascii

plugin issues:

- https://github.com/shanielh/broccoli-swig/blob/a906828df32342f3d9f4405fcb6d2895169290bf/index.js#L30
- broccoli dependency in https://github.com/zaius/broccoli-concat/blob/master/index.js
- broken caching in https://github.com/quandl/broccoli-pages


after beta:

- extract bower
- disable overwriting in MergedTree
- fix es6-concatenator API
- ? extract caching logic
- purge cache
- md5 small files



- check "file not found" errors in error page

- display component timing
- LATER source maps
- NEVER? fingerprinting


- yeoman


// how to reference images (namespaced) vs assets (URLs)?
// copier (against namespaces, transforms broccoli:/ urls)
// then fingerprinter (resolves broccoli:/ urls)




- tree
  map dirs into merged src
  apply transformers
- treereader
  map trees into merged src
  apply transformers

- 1 class for both?
  .addTree
  .map
